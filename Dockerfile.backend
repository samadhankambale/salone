FROM python:3.12-slim

WORKDIR /app

COPY requirements-backend.txt .
RUN pip install --no-cache-dir -r requirements-backend.txt

COPY app ./app

EXPOSE 8000

CMD ["sh", "-c", "python -m app.create_tables && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"]
